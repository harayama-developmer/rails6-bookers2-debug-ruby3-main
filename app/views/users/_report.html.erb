<h3>投稿数の前日比・前週比</h3>
<table class="table table-bordered">
  <thead>
    <tr>
      <th>今日の投稿数</th>
      <th>前日の投稿数</th>
      <th>前日比</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><%= user.books.created_today.size %></td>
      <td><%= user.books.created_yesterday.size %></td>
      <td><%= percentage(user.books.created_today.size, user.books.created_yesterday.size) %></td>
    </tr>
  </tbody>
</table>
<table class="table table-bordered">
  <thead>
    <tr>
      <th>今週の投稿数</th>
      <th>先週の投稿数</th>
      <th>前週比</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><%= user.books.created_this_week.size %></td>
      <td><%= user.books.created_last_week.size %></td>
      <td><%= percentage(user.books.created_this_week.size, user.books.created_last_week.size) %></td>
    </tr>
  </tbody>
</table>
